## Packages
recharts | Data visualization for dashboard charts
date-fns | Date formatting and manipulation
framer-motion | Smooth animations for page transitions and UI elements

## Notes
Auth is handled via Replit Auth blueprint.
Frontend redirects to /api/login for authentication.
API routes follow the @shared/routes contract.
